@page

@using Sales.Utility

@if (User.IsInRole(SD.ManagerRole))
{
    <div class="border align-self-sm-baseline container">
        <div class="row">
            <div class="col-6">
                <h3 class="text-dark">Lista e Perdoruesve</h3>
            </div>
            <div class="col-6 text-right">
                <a asp-page="~/Account/Register" class="btn btn-dark"><i class="fas fa-arrow-circle-right"></i> &nbsp; Shto</a>
            </div>
        </div>
        <br />
        <br />
        <table id="DT_load" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>Emri i Plote</th>
                    <th>Email</th>
                    <th>Telefoni</th>
                    <th></th>
                </tr>
            </thead>
        </table>
    </div>
}
else
{
    <div class="text-danger"><h1>Access Denied</h1></div>
}
@section Scripts{   
        <script src="~/js/user.js"></script>
    }